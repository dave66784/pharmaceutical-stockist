# Development Profile Configuration
# ─── OTP Security ─────────────────────────────────────────────────────────────
# This override allows developers to use a fixed OTP to bypass email verification.
# NEVER enable this in any other profile (e.g., prod, stag).
app.otp.test-override=123456

# Extra logging for development
logging.level.com.pharma=DEBUG
logging.level.org.springframework.security=DEBUG
spring.jpa.show-sql=true
